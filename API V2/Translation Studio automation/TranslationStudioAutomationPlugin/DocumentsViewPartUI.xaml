<UserControl x:Class="TranslationStudioAutomationPlugin.DocumentsViewPartUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TranslationStudioAutomationPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid x:Uid="Grid_1">
		<Grid.RowDefinitions>
			<RowDefinition x:Uid="RowDefinition_1" Height="*"/>
			<RowDefinition x:Uid="RowDefinition_2" Height="auto"/>
		</Grid.RowDefinitions>
		<ListView x:Uid="ListView_1" Grid.Row="0" ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument}"/>
		<StackPanel x:Uid="StackPanel_1" Grid.Row="1" Orientation="Horizontal">
			<Button x:Uid="Button_1" Margin="10" Content="Open" Command="{Binding OpenCommand}" />
			<Button x:Uid="Button_2" Margin="10" Content="Save" Command="{Binding SaveCommand}" IsEnabled="{Binding HasSelectedDocument}"/>
			<Button x:Uid="Button_3" Margin="10" Content="Close" Command="{Binding CloseCommand}" IsEnabled="{Binding HasSelectedDocument}"/>
			<Button x:Uid="Button_4" Margin="10" Content="Activate" Command="{Binding ActivateCommand}" IsEnabled="{Binding SelectedDocumentNotAlreadyActive}"/>
			<Button x:Uid="Button_5" Margin="10" Content="Save As" Command="{Binding SaveAsCommand}" IsEnabled="{Binding HasSelectedDocument}"/>
			<Button x:Uid="Button_6" Margin="10" Content="Save Source As" Command="{Binding SaveSourceAsCommand}" IsEnabled="{Binding HasSelectedDocument}"/>
			<Button x:Uid="Button_7" Margin="10" Content="Save Target As" Command="{Binding SaveTargetAsCommand}" IsEnabled="{Binding HasSelectedDocument}"/>
		</StackPanel>
	</Grid>
</UserControl>
